// Features reuse the accent-agnostic primitives from the CustomerStory
// pattern (see src/pages/features/index.js) so feature, use-case, and
// customer-story sections stay visually consistent. Only the feature
// registry lives here — it drives the /features landing and the footer.

export { ACCENT, FEATURES } from './features'
